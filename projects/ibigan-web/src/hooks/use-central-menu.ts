import { CENTRAL_MENU_SIDEBAR } from '@/config/central-menu.config';
import { type MenuConfig } from '@/config/types';

export function useCentralMenu(): MenuConfig {
  return CENTRAL_MENU_SIDEBAR;
}
